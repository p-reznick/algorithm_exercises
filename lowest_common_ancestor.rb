# 236
def lowest_common_ancestor(root, p, q)
  # if root is q or p and left or right are ancestors of q or p, root is LCA
  # if left and right both are ancestors of q or p, root is LCA
  # if only left or right is an ancestor of q or p, recur into left or right
end

def is_ancestor(root, p, q)
  # returns true if root is ancestor of p or q
end
